<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".LetrasColoresdosActivity"
    android:background="@drawable/fondoo">

    <ImageView
        android:layout_width="@dimen/image_height"
        android:layout_height="@dimen/image_width"
        android:src="@drawable/xazul"
        android:id="@+id/iv_xazul"
        android:layout_above="@+id/iv_play"
        android:layout_toLeftOf="@+id/iv_uverde"
        android:layout_toStartOf="@+id/iv_uverde"
        android:onClick="Error"/>

    <ImageView
        android:layout_width="@dimen/image_height"
        android:layout_height="@dimen/image_width"
        android:src="@drawable/uverde"
        android:id="@+id/iv_uverde"
        android:layout_alignTop="@+id/iv_smorada"
        android:layout_toLeftOf="@+id/iv_smorada"
        android:layout_toStartOf="@+id/iv_smorada"
        android:onClick="Error"/>

    <ImageView
        android:layout_width="@dimen/image_height"
        android:layout_height="@dimen/image_width"
        android:src="@drawable/smodara"
        android:id="@+id/iv_smorada"
        android:layout_alignTop="@+id/iv_qcafe"
        android:layout_toLeftOf="@+id/iv_qcafe"
        android:layout_toStartOf="@+id/iv_qcafe"
        android:onClick="Error"/>

    <ImageView
        android:layout_width="@dimen/image_height"
        android:layout_height="@dimen/image_width"
        android:src="@drawable/qcafe"
        android:id="@+id/iv_qcafe"
        android:layout_above="@+id/iv_play"
        android:layout_alignParentRight="true"
        android:layout_alignParentEnd="true"
        android:layout_marginRight="42dp"
        android:layout_marginEnd="42dp"
        android:onClick="Next"/>
    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Regresar"
        android:id="@+id/bt_regresar"
        android:layout_alignBottom="@+id/iv_play"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:onClick="Back"/>
    <ImageView
        android:layout_width="@dimen/image_width"
        android:layout_height="@dimen/image_height"
        android:src="@drawable/play"
        android:id="@+id/iv_play"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:onClick="Play"/>

</RelativeLayout>
